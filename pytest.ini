[pytest]
markers =
    fast: fast tests suitable for local smoke checks and CI.
    slow: slow tests that may run pipelines, OCR, training, or large reports.
    integration: tests that exercise multiple modules together.
    requires_external: tests that require external services or networked dependencies.
