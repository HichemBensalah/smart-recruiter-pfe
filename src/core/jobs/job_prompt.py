"""Reserved for future LLM-based job description prompting.

This first version of Module 2-Bis is intentionally rule-based and does not
call Groq or Ollama. The helper remains here so the module can evolve later
without changing its public layout.
"""

JOB_PROFILE_PROMPT_PLACEHOLDER = (
    "Future prompt template for converting a raw job description into a "
    "CanonicalJobProfile."
)
